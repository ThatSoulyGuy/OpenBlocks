package com.openblocks.canvas;

import net.minecraft.world.item.Item;

/**
 * A squeegee that removes paint from canvas blocks.
 * Right-click a canvas face to clear it; sneak-click to clear all faces.
 */
public class SqueegeeItem extends Item {

    public SqueegeeItem(Properties properties) {
        super(properties);
    }
}
